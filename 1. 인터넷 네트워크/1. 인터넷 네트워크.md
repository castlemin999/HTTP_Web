# 인터넷 네트워크

## 인터넷 통신

클라이언트에서 전송한 데이터(text, img, file …)는 중간에 수많은 노드를 거쳐 서버에 도달.

### IP (인터넷 프로토콜)

인터넷을 통해 클라이언트에서 서버에 도달하기 위한 최소한의 규약

- 지정한 IP 주소에 데이터 전달
- Packet(Package + Bucket)이라는 통신 단위로 데이터 전달

## IP 패킷 정보

- [출발지IP + 목적지IP + 기타 + [전송 데이터]]

## IP 프로토콜의 한계

1. 비연결성
- 패킷을 받을 대상이 없거나 서비스 불능 상태여도 패킷 전송
- 존재하지 않는 목적지에 보내도 전송하고 정상적으로 보내졌는지 확인 할 수가 없음.

1. 비신뢰성
- 노드를 타고 패킷이 이동중에 소실 돼도 알수가 없음.
- 목적지에 패킷이 정상적으로 도달했으나 순서가 보장 되지 않는다.
정상 : ‘I’ + ‘am’ + ‘Groot’
비정상 : ‘Groot’ + ‘I’ + ‘am’

1. 프로그램 구분
- 같은 IP를 사용하는 서버에서 통신하는 애플리케이션이 둘 이상일때 통제를 할 수 없음.

## TCP (Transmission Control Protocol)

신뢰할 수 있는 전송 제어 프로토콜 (연결형 서비스)

- 3 Way Handshake - 연결 가능 여부 판단 후 데이터 전송
- 데이터 전달 보증 - 누락, 소실 여부 판단
- 순서 보장 - 잘못된 순서부터 다시 요청

## TCP/IP 패킷 정보

- [출발지IP + 목적지IP + 기타 + [ 출발지PORT + 목적지 PORT + 전송 제어 + 순서 + 검증정보 + [전송 데이터]]]

## TCP 3 Way Handshake

- SYN : 접속 요청
- ACK : 요청 수락

### 순서

1. 클라이언트 —(SYN요청)→ 서버
2. 클라이언트 ←(SYN요청 + ACK수락)— 서버
3. 클라이언트 —(ACK수락 + 데이터)→ 서버

## UDP(User Datagram Protocol)

사용자 데이터그램 프로토콜 (비연결형 서비스)

- 기능이 거의 없음 (백지)
- TCP 3 Way Handshake 사용 X
- 데이터 전달 보증 X
- 순서 보장 X
- 단순하고 빠름

→ IP와 거의 같음 + PORT + 체크섬

<aside>
💡 체크섬(checksum)이란 네트워크를 통해서 전송된 데이터의 값이 변경되었는지(무결성)를 검사하는 값으로, 무결성을 통해서 네트워크를 통해서 수신된 데이터에 오류가 없는지 여부를 확인.

</aside>

### UDP 왜 씀?

TCP를 활용하여 최적화 하기엔 불가능하고 UDP를 활용하여 내가 원하는 대로 만들 수 있음.
또 데이터 전달, 순서가 보장 되지 않지만 단순하고 빠르기 때문에 연속성과 성능에 중요시 되는 서비스에서 사용됨HTTP3 에서는 UDP를 사용함